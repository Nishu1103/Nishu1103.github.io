---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE, SITE_AUTHOR, SITE_EMAIL, SITE_GITHUB, SITE_LINKEDIN } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`About - ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<div class="terminal-window">
				<div class="terminal-header">
					<div class="terminal-buttons">
						<span class="btn btn-red"></span>
						<span class="btn btn-yellow"></span>
						<span class="btn btn-green"></span>
					</div>
					<div class="terminal-title">about.txt</div>
				</div>
				<div class="terminal-body">
					<div class="terminal-line">
						<span class="prompt">nishant@dev:~$</span>
						<span class="command">cat about.txt</span>
					</div>
					<div class="terminal-output">
						<div class="ascii-art">
							<h1>NISHANT KUMAWAT</h1>
							<p>Full Stack Developer</p>
						</div>
						
						<div class="about-content">
							<p class="intro-text">
								Passionate about building digital experiences that matter. 
								I turn coffee into code and ideas into reality.
							</p>
							
							<div class="stats-grid">
								<div class="stat">
									<span class="stat-number">2+</span>
									<span class="stat-label">Years Coding</span>
								</div>
								<div class="stat">
									<span class="stat-number">3</span>
									<span class="stat-label">Production Systems</span>
								</div>
								<div class="stat">
									<span class="stat-number">∞</span>
									<span class="stat-label">Cups of Coffee</span>
								</div>
							</div>
						</div>
					</div>

					<div class="terminal-line">
						<span class="prompt">nishant@dev:~$</span>
						<span class="command">cat experience.txt</span>
					</div>
					<div class="terminal-output">
						<div class="experience-section">
							<div class="company-header">
								<span class="company-name">
									<a href="https://kwike.in" target="_blank" rel="noopener noreferrer">kwike.in</a>
								</span>
								<span class="role">Founding Software Engineer</span>
							</div>
							<div class="achievements">
								<div class="achievement">
									<span class="achievement-icon">🚀</span>
									<span class="achievement-text">Built 3 production systems from scratch</span>
								</div>
								<div class="achievement">
									<span class="achievement-icon">💼</span>
									<span class="achievement-text">CRM Platform</span>
								</div>
								<div class="achievement">
									<span class="achievement-icon">🛒</span>
									<span class="achievement-text">POS System</span>
								</div>
								<div class="achievement">
									<span class="achievement-icon">🌐</span>
									<span class="achievement-text">Main Website (<a href="https://kwike.in" target="_blank" rel="noopener noreferrer">kwike.in</a>)</span>
								</div>
							</div>
							<div class="focus-areas">
								<p><strong>Focus:</strong> Scalability • UX • Full-stack ownership</p>
								<p><strong>Pipeline:</strong> Architecture → API → Deployment → Monitoring</p>
							</div>
						</div>
					</div>

					<div class="terminal-line">
						<span class="prompt">nishant@dev:~$</span>
						<span class="command">cat philosophy.md</span>
					</div>
					<div class="terminal-output">
						<div class="philosophy">
							<p>
								<strong>Clean Code</strong> over clever code.<br>
								<strong>User Experience</strong> over flashy features.<br>
								<strong>Learning</strong> over knowing everything.<br>
								<strong>Collaboration</strong> over competition.
							</p>
						</div>
					</div>

					<div class="terminal-line">
						<span class="prompt">nishant@dev:~$</span>
						<span class="command">./connect.sh</span>
					</div>
					<div class="terminal-output">
						<div class="connect-links">
							<a href={`mailto:${SITE_EMAIL}`} class="link">
								📧 Email Me
							</a>
							<a href={SITE_GITHUB} target="_blank" class="link">
								🐙 GitHub
							</a>
							<a href={SITE_LINKEDIN} target="_blank" class="link">
								💼 LinkedIn
							</a>
							<a href="/blog" class="link">
								📝 Read My Blog
							</a>
						</div>
					</div>

					<div class="terminal-line">
						<span class="prompt">nishant@dev:~$</span>
						<span class="cursor">█</span>
					</div>
				</div>
			</div>
		</main>
		<Footer />
	</body>
</html>

<style>
	.terminal-output {
		margin-left: 0;
		margin-bottom: 1rem;
		color: var(--terminal-text);
	}

	.ascii-art {
		color: var(--terminal-accent);
		font-size: 0.8rem;
		margin: 1rem 0;
		text-align: center;
	}

	.ascii-art h1 {
		font-size: 1.5rem;
		margin: 0.5rem 0;
		color: var(--terminal-accent);
		font-weight: 700;
		letter-spacing: 2px;
	}

	.ascii-art p {
		margin: 0.25rem 0;
		color: var(--terminal-blue);
		font-weight: 500;
	}

	.about-content {
		margin: 2rem 0;
	}

	.intro-text {
		font-size: 1.1rem;
		text-align: center;
		color: var(--terminal-text);
		margin: 1.5rem 0;
		font-style: italic;
	}

	.stats-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1rem;
		margin: 2rem 0;
	}

	.stat {
		text-align: center;
		padding: 1rem;
		background: var(--terminal-bg);
		border-radius: 6px;
		border: 1px solid var(--terminal-border);
	}

	.stat-number {
		display: block;
		font-size: 1.8rem;
		font-weight: 700;
		color: var(--terminal-accent);
		margin-bottom: 0.25rem;
	}

	.stat-label {
		font-size: 0.8rem;
		color: var(--terminal-text-muted);
		text-transform: uppercase;
		letter-spacing: 0.5px;
	}

	.experience-section {
		margin: 1rem 0;
		padding: 1rem;
		background: var(--terminal-bg);
		border-radius: 6px;
		border-left: 4px solid var(--terminal-accent);
	}

	.company-header {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 1rem;
		padding-bottom: 0.5rem;
		border-bottom: 1px solid var(--terminal-border);
	}

	.company-name {
		color: var(--terminal-accent);
		font-weight: 700;
		font-size: 1.1rem;
	}

	.role {
		color: var(--terminal-blue);
		font-weight: 500;
		font-size: 0.9rem;
	}

	.achievements {
		margin: 1rem 0;
	}

	.achievement {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		margin-bottom: 0.5rem;
		font-size: 0.9rem;
	}

	.achievement-icon {
		font-size: 1rem;
		width: 20px;
		text-align: center;
	}

	.achievement-text {
		color: var(--terminal-text);
	}

	.focus-areas {
		margin-top: 1rem;
		padding-top: 1rem;
		border-top: 1px solid var(--terminal-border);
	}

	.focus-areas p {
		margin: 0.25rem 0;
		font-size: 0.85rem;
		color: var(--terminal-text-muted);
	}

	.focus-areas strong {
		color: var(--terminal-blue);
	}

	.skills-list {
		margin: 0.5rem 0;
	}

	.skill-item {
		display: flex;
		gap: 1rem;
		margin-bottom: 0.5rem;
		font-size: 0.9rem;
		align-items: center;
	}

	.skill-category {
		color: var(--terminal-blue);
		font-weight: 600;
		width: 80px;
	}

	.skill-tech {
		color: var(--terminal-text);
	}

	.philosophy {
		margin: 1rem 0;
		padding: 1rem;
		background: var(--terminal-bg);
		border-radius: 6px;
		border-left: 4px solid var(--terminal-accent);
	}

	.philosophy p {
		margin: 0;
		line-height: 1.8;
	}

	.philosophy strong {
		color: var(--terminal-accent);
	}

	.connect-links {
		display: flex;
		flex-wrap: wrap;
		gap: 1rem;
		margin: 1rem 0;
	}

	.link {
		color: var(--terminal-blue);
		text-decoration: none;
		padding: 0.5rem 1rem;
		border: 1px solid var(--terminal-border);
		border-radius: 4px;
		background: var(--terminal-bg);
		transition: all 0.2s ease;
		font-size: 0.9rem;
	}

	.link:hover {
		background: var(--terminal-surface-alt);
		border-color: var(--terminal-blue);
		color: var(--terminal-blue-light);
	}


	@media (max-width: 768px) {
		.terminal-body {
			padding: 1rem;
			font-size: 0.85rem;
		}

		.ascii-art h1 {
			font-size: 1.2rem;
		}

		.stats-grid {
			grid-template-columns: 1fr;
			gap: 0.5rem;
		}

		.stat-number {
			font-size: 1.5rem;
		}

		.company-header {
			flex-direction: column;
			align-items: flex-start;
			gap: 0.25rem;
		}

		.achievement {
			flex-direction: column;
			align-items: flex-start;
			gap: 0.25rem;
		}

		.skill-item {
			flex-direction: column;
			align-items: flex-start;
			gap: 0.25rem;
		}

		.skill-category {
			width: auto;
		}

		.connect-links {
			flex-direction: column;
		}
	}
</style>